.mu-calendar {
  flex-direction: column;
  min-height: 240px;
  min-width: 280px;
  font-size: $calendarFontSize;
  background: $commonBackground;
  border: 1px solid $commonBorderColor;
  overflow: hidden;
  user-select: none;

  &,
  & > .mu-calendar-header,
  & > .mu-calendar-grid,
  & > .mu-week-header,
  & .mu-calendar-row,
  & .mu-calendar-cell {
    display: flex;
    align-items: stretch;
  }

  & > .mu-calendar-header {
    align-items: center;
    height: $compactBarHeight;
    padding: 0 $halfSpacingSize;

    & > .mu-calendar-title {
      margin-right: auto;
      font-size: $calendarTitleFontSize;
      font-weight: 600;
      color: $primaryColor;
      cursor: pointer;
    }
  }

  & > .mu-week-header {
    align-items: center;
    height: $compactBarHeight;
    font-weight: 600;
    color: $grey;
    border-top: 1px solid $primaryColor;

    & > * {
      flex-grow: 1;
      width: 1px;
      text-align: center;
    }
  }

  & > .mu-calendar-grid {
    flex-direction: column;
    flex-grow: 1;
    height: 1px;
    border-top: 1px solid $primaryColor;
    overflow: visible;

    & > .mu-calendar-row {
      height: 1px;
      flex-grow: 1;

      & + .mu-calendar-row {
        border-top: 1px solid $commonGridLineColor;
      }
    }
    & .mu-calendar-cell {
      position: relative;
      align-items: center;
      justify-content: center;
      flex-grow: 1;
      width: 1px;
      color: $primaryColor;
      cursor: pointer;

      & + .mu-calendar-cell {
        border-left: 1px solid $commonGridLineColor;
      }

      &[marked]:after {
        position: absolute;
        bottom: 0;
        left: 0;
        width: 0;
        height: 0;
        content: '';
        border-bottom: 8px solid $dangerColor;
        border-right: 8px solid transparent;
      }
      &[adjacent] {
        color: $greyMinus;
      }
      &:hover {
        color: $primaryMinusColor;
        background: $commonHoverBackground;
      }
      &[present] {
        font-weight: 600;
        color: $warningColor;
      }
      &[invalid] {
        color: $commonDisabledTextColor;
        background: $commonDisabledBackground;
        cursor: default;
      }
      &[active] {
        z-index: 1;
        font-weight: 600;
        color: #fff;
        background: $primaryColor;
        box-shadow: 0px 0px 8px rgba(0, 0, 0, 0.17), 0px 0px 4px rgba(0, 0, 0, 0.35);
      }
    }
  }
}

.mu-dropdown-panel > .mu-calendar {
  height: 100%;
  border: 0;
}