.mu-toggle {
  display: inline-flex;
  align-items: center;
  height: $inputElementHeight;
  vertical-align: middle;

  & > * {
    cursor: pointer;
  }

  & > .mu-toggle-slide-bar {
    position: relative;
    width: 40px;
    height: 20px;
    border-radius: $toggleButtonHeight;
    background: $normalDarkGreyBackground;
    transition: background 0.2s ease-in-out;

    &:after {
      position: absolute;
      top: 3px;
      left: 3px;
      bottom: 3px;
      display: inline-block;
      width: 14px;
      background: #fff;
      border-radius: 7px;
      content: '';
      transition: all 0.2s ease-in-out;
    }
  }

  &[active] > .mu-toggle-slide-bar {
    background: $successColor;
    &:after {
      left: 23px;
    }
  }

  & > * + * {
    margin-left: 5px;
  }

  &[disabled] {
    & > * {
      color: $normalWeakTextColor;
      cursor: default;
    }
    & > .mu-toggle-slide-bar {
      background: $normalGreyBackground;

      &:after {
        background: $normalDisabledBackground;
      }
    }
  }
}
